import { StyleSheet, Text, View } from "react-native";
import React from "react";

const HelpSearchCard = () => {
  return (
    <View>
      <Text>HelpSearchCard</Text>
    </View>
  );
};

export default HelpSearchCard;

const styles = StyleSheet.create({
  container: {
    width: "100%",
    borderRadius: 20,
  },
});
